export POD_NAME=$(kubectl get pods --namespace monitoring  -l app.kubernetes.io/name=grafana -o jsonpath="{.items[0].metadata.name}" -o jsonpath="{.items[0].metadata.name}" )
kubectl --namespace monitoring port-forward $POD_NAME 3000